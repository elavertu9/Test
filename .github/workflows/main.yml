name: Python application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: lint
    
    runs-on: ubuntu-latest
    
    steps:
      - name: Python Linter
        uses: sunnysid3up/python-linter@v0.1-beta.7

