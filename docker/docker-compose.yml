version: '3.1'

services:
  testing:
    image: test-github-actions-image
    container_name: testing-github-actions-container
    hostname: testing-github-actions-container
    volumes:
      - type: volume
        source: /tmp/test
        target: /test-vol-dir
#      - "test-vol:/test-vol-dir"

volumes:
  test-vol:
    external: false